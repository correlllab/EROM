# General Environment
`python3.10 -m pip install -r requirements.txt --force-reinstall --no-cache-dir --user`
# SAM2
1. `cd ~`
1. `git clone https://github.com/facebookresearch/sam2.git && cd sam2`
1. `python3.10 -m pip install -e .`